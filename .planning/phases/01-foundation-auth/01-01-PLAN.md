---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tailwind.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - .env.local
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03

must_haves:
  truths:
    - "Next.js 16 project runs with App Router and TypeScript"
    - "Supabase client configured for browser and server environments"
    - "Design tokens available as CSS variables (brand colors, typography, spacing)"
    - "Tailwind CSS uses design tokens via @theme configuration"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design tokens as CSS custom properties"
      min_lines: 30
    - path: "src/lib/supabase/client.ts"
      provides: "Browser Supabase client with anon key"
      exports: ["createClient"]
    - path: "src/lib/supabase/server.ts"
      provides: "Server Supabase client with cookie handling"
      exports: ["createClient"]
    - path: ".env.local"
      provides: "Supabase environment variables template"
  key_links:
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "@theme CSS variables"
      pattern: "color-brand-red|color-brand-green"
    - from: "src/lib/supabase/server.ts"
      to: "src/app/globals.css"
      via: "cookies from next/headers"
      pattern: "createServerClient|cookies"
---

<objective>
Set up Next.js 16 project with App Router, TypeScript, Tailwind CSS, and Supabase client configuration with design tokens.
</objective>

<execution_context>
@C:/Users/ob-ta/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@RESEURCES/style-tokens.md
@RESEURCES/brand-guidelines.md
</context>

<tasks>

<task type="auto">
  <name>Initialize Next.js project with App Router and TypeScript</name>
  <files>package.json, next.config.ts, tsconfig.json</files>
  <read_first>
    - package.json (check if project already initialized)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (Standard Stack section for version details)
  </read_first>
  <action>
    Run: npx create-next-app@latest prostep2market --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm

    If project already exists in current directory, skip initialization and verify:
    1. Package.json contains next, react, react-dom dependencies
    2. next.config.ts exists with App Router config
    3. tsconfig.json has "compilerOptions.paths" with "@/*": ["./src/*"]
  </action>
  <verify>
    <automated>grep -c '"next"' package.json && grep -c '"react"' package.json</automated>
  </verify>
  <done>
    Next.js project with App Router and TypeScript configured
  </done>
  <acceptance_criteria>
    - package.json contains "next": "^16" or higher
    - tsconfig.json has "compilerOptions.jsx": "preserve" or "react-jsx"
    - tsconfig.json has "@/*": ["./src/*"] path alias
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Install Supabase and required dependencies</name>
  <files>package.json, src/lib/supabase/client.ts, src/lib/supabase/server.ts</files>
  <read_first>
    - package.json (check current dependencies)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (Standard Stack for versions: @supabase/ssr@0.10.3, @supabase/supabase-js@2.105.4)
  </read_first>
  <action>
    Install dependencies:
    ```
    npm install @supabase/supabase-js@2.105.4 @supabase/ssr@0.10.3 lucide-react recharts react-joyride
    ```

    Create src/lib/supabase/client.ts:
    ```typescript
    import { createBrowserClient } from '@supabase/ssr'

    export function createClient() {
      return createBrowserClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
      )
    }
    ```

    Create src/lib/supabase/server.ts:
    ```typescript
    import { createServerClient } from '@supabase/ssr'
    import { cookies } from 'next/headers'

    export async function createClient() {
      const cookieStore = await cookies()

      return createServerClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
        {
          cookies: {
            getAll() {
              return cookieStore.getAll()
            },
            setAll(cookiesToSet) {
              cookiesToSet.forEach(({ name, value, options }) => {
                cookieStore.set(name, value, options)
              })
            },
          },
        }
      )
    }
    ```
  </action>
  <verify>
    <automated>grep -c "@supabase" package.json && test -f src/lib/supabase/client.ts && test -f src/lib/supabase/server.ts</automated>
  </verify>
  <done>
    Supabase clients configured for browser and server environments
  </done>
  <acceptance_criteria>
    - package.json contains "@supabase/supabase-js" and "@supabase/ssr"
    - src/lib/supabase/client.ts exports createBrowserClient
    - src/lib/supabase/server.ts exports createServerClient with cookie handling
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement design tokens as CSS variables and configure Tailwind</name>
  <files>src/app/globals.css, tailwind.config.ts</files>
  <read_first>
    - src/app/globals.css (check if exists)
    - tailwind.config.ts (check Tailwind version and current config)
    - RESEURCES/style-tokens.md (for exact color values: #E53935, #2E7D32, #0B0B0B, #FFFFFF, #F5F7FA, #1F2933)
  </read_first>
  <action>
    Create/update src/app/globals.css with design tokens:
    ```css
    @import "tailwindcss";

    @theme {
      /* Brand Colors */
      --color-brand-red: #E53935;
      --color-brand-green: #2E7D32;
      --color-brand-black: #0B0B0B;
      --color-white: #FFFFFF;
      
      /* Supporting Colors */
      --color-dark-gray: #1F2933;
      --color-muted-gray: #F5F7FA;
      --color-muted-red: #F8D7D6;
      --color-muted-green: #E8F5E9;
      
      /* Border Radius */
      --radius-sm: 6px;
      --radius-md: 12px;
      
      /* Typography */
      --font-sans: 'Inter', 'Poppins', system-ui, sans-serif;
    }

    body {
      font-family: var(--font-sans);
      color: var(--color-brand-black);
      background-color: var(--color-white);
    }
    ```

    Note: If Tailwind v3 is used (check tailwind.config.ts), use traditional config instead:
    Update tailwind.config.ts:
    ```typescript
    import type { Config } from "tailwindcss";

    const config: Config = {
      content: [
        "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
        "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
        "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
      ],
      theme: {
        extend: {
          colors: {
            'brand-red': '#E53935',
            'brand-green': '#2E7D32',
            'brand-black': '#0B0B0B',
            'dark-gray': '#1F2933',
            'muted-gray': '#F5F7FA',
            'muted-red': '#F8D7D6',
            'muted-green': '#E8F5E9',
          },
          borderRadius: {
            'sm': '6px',
            'md': '12px',
          },
          fontFamily: {
            sans: ['Inter', 'Poppins', 'system-ui', 'sans-serif'],
          },
        },
      },
      plugins: [],
    };
    export default config;
    ```
  </action>
  <verify>
    <automated>grep -c "brand-red" src/app/globals.css || grep -c "brand-red" tailwind.config.ts</automated>
  </verify>
  <done>
    Design tokens available as CSS variables, Tailwind configured with brand colors
  </done>
  <acceptance_criteria>
    - src/app/globals.css defines --color-brand-red: #E53935
    - src/app/globals.css defines --color-brand-green: #2E7D32
    - src/app/globals.css defines --color-brand-black: #0B0B0B
    - src/app/globals.css defines --radius-sm: 6px and --radius-md: 12px
    - Tailwind config maps colors or @theme defines them
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create .env.local template with Supabase credentials</name>
  <files>.env.local</files>
  <read_first>
    - .env.local (check if exists)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md (Standard Stack section for env var names)
  </read_first>
  <action>
    Create .env.local:
    ```
    # Supabase Configuration
    NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
    ```
    
    Note: The executor needs real Supabase credentials. Create placeholder if not provided.
  </action>
  <verify>
    <automated>grep -c "NEXT_PUBLIC_SUPABASE" .env.local</automated>
  </verify>
  <done>
    .env.local contains Supabase environment variable template
  </done>
  <acceptance_criteria>
    - .env.local contains NEXT_PUBLIC_SUPABASE_URL
    - .env.local contains NEXT_PUBLIC_SUPABASE_ANON_KEY
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase | Untrusted input (email/password) sent to Supabase Auth |
| Server → Supabase | Server-side requests via @supabase/ssr |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local file | mitigate | Never commit .env.local; add to .gitignore; use env vars on Vercel |
| T-01-02 | Tampering | CSS design tokens | accept | Tokens are read-only; no user input affects them |
</threat_model>

<verification>
- npm run build completes successfully
- All imports resolve without errors
- Tailwind classes apply brand colors correctly
</verification>

<success_criteria>
- Next.js project runs on localhost:3000
- Supabase clients import without errors
- CSS variables available in browser DevTools
- Tailwind classes like bg-brand-red, text-brand-black work
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>