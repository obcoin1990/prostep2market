---
phase: 06-edge-score
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/edge-score/types.ts
  - src/lib/edge-score/calculations/discipline.ts
  - src/lib/edge-score/calculations/risk.ts
  - src/lib/edge-score/calculations/emotional-stability.ts
  - src/lib/edge-score/calculations/consistency.ts
  - src/lib/edge-score/calculations/strategy-adherence.ts
  - src/lib/edge-score/calculations/composite.ts
  - src/lib/edge-score/ranking.ts
  - src/lib/edge-score/tips.ts
autonomous: true
requirements:
  - EDGE-01
  - EDGE-02
  - EDGE-03
  - EDGE-04
  - EDGE-05
  - JRNL-07
---

<objective>
Implement the Score Calculation Engine for Edge Score system. Creates all scoring algorithms for five score components (discipline, risk, emotional stability, consistency, strategy adherence), composite calculation, ranking logic, and quick tips mapping.
</objective>

<execution_context>
@C:/Users/ob-ta/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ob-ta/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-edge-score/06-CONTEXT.md
@.planning/phases/06-edge-score/06-RESEARCH.md
@.planning/phases/03-trade-journal/03-CONTEXT.md
@.planning/phases/04-ai-trade-intelligence/04-CONTEXT.md
@.planning/phases/05-risk-guardian/05-CONTEXT.md
</context>

<interfaces>
<!-- Key interfaces from 06-CONTEXT.md - Score Breakdown -->
interface EdgeScoreBreakdown {
  disciplineScore: number;      // 0-100
  riskScore: number;             // 0-100
  emotionalStabilityScore: number; // 0-100
  consistencyScore: number;      // 0-100
  strategyAdherenceScore: number; // 0-100
  compositeScore: number;        // 0-100
}

interface ScoreHistory {
  date: Date;
  disciplineScore: number;
  riskScore: number;
  emotionalStabilityScore: number;
  consistencyScore: number;
  strategyAdherenceScore: number;
  compositeScore: number;
  rank: Rank;
}

type Rank = 'beginner' | 'developing' | 'consistent' | 'advanced' | 'elite';

const SCORE_WEIGHTS = {
  discipline: 0.25,
  risk: 0.25,
  emotionalStability: 0.20,
  consistency: 0.15,
  strategyAdherence: 0.15
};
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Type Definitions</name>
  <files>
    - src/lib/edge-score/types.ts
  </files>
  <action>
    Create src/lib/edge-score/types.ts with:
    - EdgeScoreBreakdown interface (disciplineScore, riskScore, emotionalStabilityScore, consistencyScore, strategyAdherenceScore, compositeScore)
    - ScoreHistory interface with date, scores, and rank
    - Rank type: 'beginner' | 'developing' | 'consistent' | 'advanced' | 'elite'
    - SCORE_WEIGHTS constant object
    - SparklineData interface for trend visualization
    - Export all types and constants
  </action>
  <verify>
    <automated>ls src/lib/edge-score/types.ts 2>/dev/null && echo "types.ts exists"</automated>
  </verify>
  <done>
    Types file exists with EdgeScoreBreakdown, ScoreHistory, Rank, SCORE_WEIGHTS, SparklineData exported
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Score Algorithms</name>
  <files>
    - src/lib/edge-score/calculations/discipline.ts
    - src/lib/edge-score/calculations/risk.ts
    - src/lib/edge-score/calculations/emotional-stability.ts
    - src/lib/edge-score/calculations/consistency.ts
    - src/lib/edge-score/calculations/strategy-adherence.ts
    - src/lib/edge-score/calculations/composite.ts
  </files>
  <action>
    Create each algorithm file using formulas from 06-CONTEXT.md:

    **discipline.ts:**
    - Formula: (ruleAdherence * 0.6 + journalingConsistency * 0.4) * 100
    - ruleAdherence = trades with pre_trade_plan_adherence >= 4 / total trades
    - journalingConsistency = days with at least 1 trade logged / total days

    **risk.ts:**
    - Formula: baseScore - (lotSizeVariance * 10) - (drawdownRatio * 20)
    - baseScore starts at 100
    - lotSizeVariance = std dev of lot size / average lot size
    - drawdownRatio = maxDrawdown / accountSize

    **emotional-stability.ts:**
    - Formula: (calmTradeRatio * 50) + (recoveryBonus * 30) + (alertPenalty * 20)
    - calmTradeRatio = trades after alerts that followed guidelines / total trades after alerts
    - recoveryBonus for avg recovery time < 30 minutes (10pts per 5min under)
    - alertPenalty = penalize when > 3 alerts in session

    **consistency.ts:**
    - Formula: (streakBonus * 40) + (lowVarianceBonus * 30) + (sessionConsistency * 30)
    - streakBonus for consecutive winning days
    - lowVarianceBonus for return_std < 0.1
    - sessionConsistency for similar session lengths

    **strategy-adherence.ts:**
    - Formula: (planAdherence * 0.5 + reviewCompletion * 0.3 + strategyRules * 0.2) * 100
    - planAdherence = trades with pre-trade plan >= 4
    - reviewCompletion = trades with post-trade review / total trades
    - strategyRules = trades following all rules / total trades

    **composite.ts:**
    - Calculate weighted average using SCORE_WEIGHTS
    - Clamp result to 0-100 range
  </action>
  <verify>
    <automated>ls src/lib/edge-score/calculations/*.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    All 6 algorithm files exist with correct formulas from CONTEXT.md
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Ranking and Tips</name>
  <files>
    - src/lib/edge-score/ranking.ts
    - src/lib/edge-score/tips.ts
  </files>
  <action>
    **ranking.ts:**
    - Create getRank(compositeScore: number): Rank function
    - Map scores to ranks:
      - 0-20: beginner
      - 21-40: developing
      - 41-60: consistent
      - 61-80: advanced
      - 81-100: elite
    - Include rankConfig with label, color, icon for each rank

    **tips.ts:**
    - Create tipsByComponent mapping
    - For each component (discipline, risk, emotionalStability, consistency, strategyAdherence):
      - 0-20: critical tip
      - 21-40: improvement tip
      - 41-60: good tip
      - 61-80: strong tip
      - 81-100: elite tip
    - Create generateQuickTips(scores: EdgeScoreBreakdown): string[] function
    - Returns tips for 2 lowest-scoring components
  </action>
  <verify>
    <automated>ls src/lib/edge-score/ranking.ts src/lib/edge-score/tips.ts 2>/dev/null</automated>
  </verify>
  <done>
    ranking.ts exports getRank function with correct thresholds
    tips.ts exports generateQuickTips returning tips for lowest 2 components
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Index Exports</name>
  <files>
    - src/lib/edge-score/index.ts
  </files>
  <action>
    Create src/lib/edge-score/index.ts that exports:
    - All types from types.ts
    - calculateDisciplineScore from calculations/discipline
    - calculateRiskScore from calculations/risk
    - calculateEmotionalStabilityScore from calculations/emotional-stability
    - calculateConsistencyScore from calculations/consistency
    - calculateStrategyAdherenceScore from calculations/strategy-adherence
    - calculateCompositeScore from calculations/composite
    - getRank from ranking
    - generateQuickTips from tips
    - SCORE_WEIGHTS constant
  </action>
  <verify>
    <automated>cat src/lib/edge-score/index.ts | grep -c "^export"</automated>
  </verify>
  <done>
    Index exports all calculation functions, ranking, tips, and constants
  </done>
</task>

</tasks>

<must_haves>
  truths:
    - "Score calculation algorithms match formulas in 06-CONTEXT.md exactly"
    - "All five component scores calculate to 0-100 range"
    - "Composite score is weighted average using SCORE_WEIGHTS"
    - "Rank correctly maps from composite score to Beginner-Elite"
    - "Quick tips generate for lowest-scoring components"
  artifacts:
    - path: "src/lib/edge-score/types.ts"
      provides: "Type definitions for Edge Score system"
      min_lines: 25
    - path: "src/lib/edge-score/calculations/*.ts"
      provides: "Score algorithms for 5 components + composite"
      min_lines: 30
    - path: "src/lib/edge-score/ranking.ts"
      provides: "Rank determination from composite score"
      min_lines: 20
    - path: "src/lib/edge-score/tips.ts"
      provides: "Quick tips mapping and generation"
      min_lines: 30
    - path: "src/lib/edge-score/index.ts"
      provides: "Single export point for edge-score library"
      min_lines: 15
  key_links:
    - from: "src/lib/edge-score/calculations/composite.ts"
      to: "src/lib/edge-score/types.ts"
      via: "SCORE_WEIGHTS import"
      pattern: "import.*SCORE_WEIGHTS"
    - from: "src/lib/edge-score/tips.ts"
      to: "src/lib/edge-score/types.ts"
      via: "EdgeScoreBreakdown type"
      pattern: "EdgeScoreBreakdown"
</must_haves>

<verification>
[Check each algorithm file exists and exports calculation function]
[Verify ranking thresholds match 06-CONTEXT.md: 0-20/21-40/41-60/61-80/81-100]
[Verify tips generate for lowest 2 components sorted by score]
</verification>

<success_criteria>
- [ ] All 6 algorithm files implement formulas from 06-CONTEXT.md
- [ ] Composite score uses SCORE_WEIGHTS: discipline 0.25, risk 0.25, emotionalStability 0.20, consistency 0.15, strategyAdherence 0.15
- [ ] Ranking maps correctly: Beginner(0-20), Developing(21-40), Consistent(41-60), Advanced(61-80), Elite(81-100)
- [ ] Quick tips generate for 2 lowest components
- [ ] Index exports all functions for consumption by other phases
</success_criteria>

<output>
After completion, create `.planning/phases/06-edge-score/06-01-SUMMARY.md`
</output>