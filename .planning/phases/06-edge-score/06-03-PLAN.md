---
phase: 06-edge-score
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
  - 06-02-PLAN.md
files_modified:
  - src/components/dashboard/EdgeScoreCard.tsx
  - src/components/dashboard/ScoreSparkline.tsx
  - src/components/dashboard/RankBadge.tsx
  - src/components/dashboard/ScoreBreakdown.tsx
autonomous: true
requirements:
  - EDGE-06
  - EDGE-07
  - EDGE-08
  - EDGE-09
---

<objective>
Create Dashboard Widgets for Edge Score display. Implements Edge Score card with composite score, breakdown bars, sparkline, rank badge, and quick tips display.
</objective>

<execution_context>
@C:/Users/ob-ta/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ob-ta/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-edge-score/06-CONTEXT.md
@.planning/phases/06-edge-score/06-RESEARCH.md
@.planning/phases/06-edge-score/06-01-PLAN.md
@.planning/phases/06-edge-score/06-02-PLAN.md
</context>

<interfaces>
<!-- Interfaces from prior plans -->
interface EdgeScoreBreakdown {
  disciplineScore: number;
  riskScore: number;
  emotionalStabilityScore: number;
  consistencyScore: number;
  strategyAdherenceScore: number;
  compositeScore: number;
}

interface SparklineData {
  date: string;
  score: number;
}

type Rank = 'beginner' | 'developing' | 'consistent' | 'advanced' | 'elite';
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Sparkline Component</name>
  <files>
    - src/components/dashboard/ScoreSparkline.tsx
  </files>
  <action>
    Create src/components/dashboard/ScoreSparkline.tsx:
    - Import LineChart, Line, ResponsiveContainer from recharts
    - Accept data: SparklineData[] prop
    - Accept width (default 120), height (default 40), color (default indigo-500)
    - Use isAnimationActive={false} for performance
    - No axes, no grid, no labels - pure sparkline
    - Handle empty data (return empty state or zeros)
    - TypeScript types for props
  </action>
  <verify>
    <automated>ls src/components/dashboard/ScoreSparkline.tsx 2>/dev/null</automated>
  </verify>
  <done>
    Sparkline component renders minimal line chart without axes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Rank Badge Component</name>
  <files>
    - src/components/dashboard/RankBadge.tsx
  </files>
  <action>
    Create src/components/dashboard/RankBadge.tsx:
    - Accept rank: Rank prop
    - Define rankConfig mapping:
      - beginner: gray-500, label "Beginner", icon "sprout"
      - developing: blue-500, label "Developing Trader", icon "trending-up"
      - consistent: green-500, label "Consistent Trader", icon "check-circle"
      - advanced: purple-500, label "Advanced Trader", icon "award"
      - elite: yellow-500 to amber-600 gradient, label "Elite Trader", icon "crown"
    - Import appropriate icons from lucide-react
    - Style with color-coded background
    - Use cn() for conditional classes if available
  </action>
  <verify>
    <automated>ls src/components/dashboard/RankBadge.tsx 2>/dev/null</automated>
  </verify>
  <done>
    Rank badge displays with correct color and label per rank
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Score Breakdown Component</name>
  <files>
    - src/components/dashboard/ScoreBreakdown.tsx
  </files>
  <action>
    Create src/components/dashboard/ScoreBreakdown.tsx:
    - Accept scores: EdgeScoreBreakdown prop
    - Render 5 horizontal score bars:
      - Discipline, Risk, Emotional Stability, Consistency, Strategy Adherence
    - Each bar shows: label, numeric score, horizontal progress bar
    - Progress bar width = (score / 100) * 100%
    - Color-code bars: discipline blue, risk red, emotional green, consistency purple, strategy orange
    - Use Shadcn/ui Progress component if available
  </action>
  <verify>
    <automated>ls src/components/dashboard/ScoreBreakdown.tsx 2>/dev/null</automated>
  </verify>
  <done>
    Score breakdown renders 5 component bars with scores
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Edge Score Card</name>
  <files>
    - src/components/dashboard/EdgeScoreCard.tsx
  </files>
  <action>
    Create src/components/dashboard/EdgeScoreCard.tsx:
    - Fetch score data on mount from /api/scores
    - Fetch history data from /api/scores/history
    - Display large composite score (0-100) with circular progress or large number
    - Include ScoreSparkline for 30-day trend
    - Include RankBadge component
    - Include ScoreBreakdown component (expandable)
    - Import generateQuickTips from @/lib/edge-score
    - Show 1-2 quick tips below score
    - Handle loading and error states
    - Handle empty score (new user) with onboarding prompt
  </action>
  <verify>
    <automated>ls src/components/dashboard/EdgeScoreCard.tsx 2>/dev/null</automated>
  </verify>
  <done>
    Edge Score card displays composite, sparkline, rank, breakdown, tips
  </done>
</task>

</tasks>

<must_haves>
  truths:
    - "Sparkline renders 30-day score trend without axes"
    - "Rank badge shows correct color and label per rank"
    - "Score breakdown displays all 5 component scores with bars"
    - "Edge Score card fetches and displays all score data"
    - "Quick tips generate from lowest components"
  artifacts:
    - path: "src/components/dashboard/ScoreSparkline.tsx"
      provides: "30-day trend sparkline visualization"
      min_lines: 25
    - path: "src/components/dashboard/RankBadge.tsx"
      provides: "Rank display with color coding"
      min_lines: 25
    - path: "src/components/dashboard/ScoreBreakdown.tsx"
      provides: "5 component score bars"
      min_lines: 30
    - path: "src/components/dashboard/EdgeScoreCard.tsx"
      provides: "Main dashboard widget"
      min_lines: 50
  key_links:
    - from: "src/components/dashboard/EdgeScoreCard.tsx"
      to: "/api/scores"
      via: "fetch on mount"
      pattern: "fetch.*api/scores"
    - from: "src/components/dashboard/EdgeScoreCard.tsx"
      to: "src/lib/edge-score"
      via: "generateQuickTips import"
      pattern: "import.*generateQuickTips"
    - from: "src/components/dashboard/ScoreSparkline.tsx"
      to: "recharts"
      via: "LineChart import"
      pattern: "import.*recharts"
</must_haves>

<verification>
[Sparkline renders with isAnimationActive={false}]
[Rank badge uses correct colors per rank config]
[Score breakdown shows 5 bars]
[Edge score card fetches from API routes]
[Quick tips display from edge-score library]
</verification>

<success_criteria>
- [ ] ScoreSparkline renders sparkline from history data
- [ ] RankBadge displays correct color/icon/label per rank
- [ ] ScoreBreakdown shows 5 component bars
- [ ] EdgeScoreCard fetches current score from /api/scores
- [ ] EdgeScoreCard fetches history from /api/scores/history
- [ ] Quick tips generate from lowest-scoring components
</success_criteria>

<output>
After completion, create `.planning/phases/06-edge-score/06-03-SUMMARY.md`
</output>