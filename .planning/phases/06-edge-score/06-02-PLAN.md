---
phase: 06-edge-score
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/006_add_edge_scores.sql
  - src/app/api/scores/route.ts
  - src/app/api/scores/history/route.ts
autonomous: true
requirements:
  - EDGE-06
  - EDGE-07
---

<objective>
Create Database Schema and API Routes for Edge Score storage and retrieval. Implements edge_scores and leaderboard_settings tables, API endpoints for score calculation trigger, and score history retrieval.
</objective>

<execution_context>
@C:/Users/ob-ta/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ob-ta/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-edge-score/06-CONTEXT.md
@.planning/phases/06-edge-score/06-RESEARCH.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
</context>

<interfaces>
<!-- Database schema from 06-CONTEXT.md -->
CREATE TABLE edge_scores (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  date DATE NOT NULL,
  discipline_score DECIMAL NOT NULL,
  risk_score DECIMAL NOT NULL,
  emotional_stability_score DECIMAL NOT NULL,
  consistency_score DECIMAL NOT NULL,
  strategy_adherence_score DECIMAL NOT NULL,
  composite_score DECIMAL NOT NULL,
  rank TEXT CHECK (rank IN ('beginner', 'developing', 'consistent', 'advanced', 'elite')),
  UNIQUE(user_id, date)
);

CREATE INDEX idx_edge_scores_user_date ON edge_scores(user_id, date DESC);

CREATE TABLE leaderboard_settings (
  user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  visibility TEXT DEFAULT 'anonymous' CHECK (visibility IN ('public', 'anonymous', 'hidden')),
  show_in_leaderboard BOOLEAN DEFAULT TRUE,
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Database Migration</name>
  <files>
    - supabase/migrations/006_add_edge_scores.sql
  </files>
  <action>
    Create supabase/migrations/006_add_edge_scores.sql:
    - Create edge_scores table with all columns from schema above
    - Add unique constraint on (user_id, date)
    - Create index idx_edge_scores_user_date on (user_id, date DESC)
    - Create leaderboard_settings table
    - Add RLS policies:
      - Users can read any edge_scores
      - Users can insert/update their own edge_scores
      - Users can read/update their own leaderboard_settings
    - Grant appropriate permissions
  </action>
  <verify>
    <automated>ls supabase/migrations/006_add_edge_scores.sql 2>/dev/null</automated>
  </verify>
  <done>
    Migration creates edge_scores and leaderboard_settings tables with RLS
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Score API Route</name>
  <files>
    - src/app/api/scores/route.ts
  </files>
  <action>
    Create src/app/api/scores/route.ts:
    - GET: Fetch current user's latest edge score
      - Query edge_scores for most recent date
      - Return EdgeScoreBreakdown with scores and rank
    - POST: Trigger score calculation for user
      - Accept optional days parameter (default 30)
      - Import score calculation functions from @/lib/edge-score
      - Query trade data, alert history, journal entries for lookback period
      - Calculate each component score
      - Compute composite score
      - Determine rank
      - Insert/Update edge_scores table
      - Return calculated scores
    - Validate user is authenticated
    - Return scores clamped to 0-100
  </action>
  <verify>
    <automated>ls src/app/api/scores/route.ts 2>/dev/null</automated>
  </verify>
  <done>
    GET returns latest score, POST calculates and stores new score
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Score History API Route</name>
  <files>
    - src/app/api/scores/history/route.ts
  </files>
  <action>
    Create src/app/api/scores/history/route.ts:
    - GET: Fetch score history for user
      - Accept days parameter (default 30, max 90)
      - Query edge_scores table ordered by date DESC
      - Return array of ScoreHistory objects
      - Format dates as ISO strings
      - Calculate SparklineData array for sparkline rendering
    - Validate user is authenticated
    - Handle empty history (return empty array, not error)
  </action>
  <verify>
    <automated>ls src/app/api/scores/history/route.ts 2>/dev/null</automated>
  </verify>
  <done>
    GET returns score history as array sorted by date descending
  </done>
</task>

</tasks>

<must_haves>
  truths:
    - "Database migration creates edge_scores and leaderboard_settings tables"
    - "RLS policies restrict access to user's own data"
    - "Score API calculates scores using edge-score library"
    - "Score history returns last N days sorted by date"
  artifacts:
    - path: "supabase/migrations/006_add_edge_scores.sql"
      provides: "Database schema for Edge Score"
      min_lines: 30
    - path: "src/app/api/scores/route.ts"
      provides: "GET latest score, POST trigger calculation"
      min_lines: 30
    - path: "src/app/api/scores/history/route.ts"
      provides: "GET score history"
      min_lines: 20
  key_links:
    - from: "src/app/api/scores/route.ts"
      to: "src/lib/edge-score"
      via: "import calculation functions"
      pattern: "import.*from.*edge-score"
    - from: "src/app/api/scores/route.ts"
      to: "supabase/migrations/006_add_edge_scores.sql"
      via: "database insert"
      pattern: "from.*edge_scores"
</must_haves>

<verification>
[Migration file creates tables with correct schema]
[Score routes handle GET and POST requests]
[History route accepts days parameter]
[RLS policies restrict to user's own data]
</verification>

<success_criteria>
- [ ] Migration creates edge_scores and leaderboard_settings with RLS
- [ ] GET /api/scores returns latest score from edge_scores table
- [ ] POST /api/scores calculates and stores new score
- [ ] GET /api/scores/history returns score array sorted by date
- [ ] All score values clamped to 0-100 range
</success_criteria>

<output>
After completion, create `.planning/phases/06-edge-score/06-02-SUMMARY.md`
</output>