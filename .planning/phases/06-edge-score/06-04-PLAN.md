---
phase: 06-edge-score
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03-PLAN.md
files_modified:
  - src/components/leaderboard/Leaderboard.tsx
  - src/components/leaderboard/QuickTips.tsx
  - src/app/api/leaderboard/route.ts
autonomous: true
requirements:
  - EDGE-08
  - EDGE-09
  - EDGE-10
  - JRNL-06
  - JRNL-08
---

<objective>
Create Leaderboard and Quick Tips components. Implements leaderboard with privacy controls, user ranking display, and contextual tips for improvement.
</objective>

<execution_context>
@C:/Users/ob-ta/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ob-ta/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-edge-score/06-CONTEXT.md
@.planning/phases/06-edge-score/06-RESEARCH.md
@.planning/phases/06-edge-score/06-03-PLAN.md
</context>

<interfaces>
<!-- Privacy settings from 06-CONTEXT.md -->
type Visibility = 'public' | 'anonymous' | 'hidden';

interface LeaderboardEntry {
  rank: number;
  displayName: string;
  compositeScore: number;
  rankLabel: string;
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Leaderboard API Route</name>
  <files>
    - src/app/api/leaderboard/route.ts
  </files>
  <action>
    Create src/app/api/leaderboard/route.ts:
    - GET: Fetch leaderboard with privacy filtering
      - Query leaderboard_settings to get visible user IDs
      - Filter out users with visibility = 'hidden'
      - Query edge_scores for latest composite_score per visible user
      - Join with users table for display name
      - Map display name based on visibility:
        - public: actual username
        - anonymous: "Trader #XXXXXX" (first 6 of UUID)
      - Order by composite_score DESC
      - Limit to top 100
      - Include user's own rank if in top 100
      - Return pagination info (hasMore, total)
    - Accept period query: weekly, monthly, all-time (default monthly)
  </action>
  <verify>
    <automated>ls src/app/api/leaderboard/route.ts 2>/dev/null</automated>
  </verify>
  <done>
    Leaderboard API returns privacy-filtered rankings
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Leaderboard Component</name>
  <files>
    - src/components/leaderboard/Leaderboard.tsx
  </files>
  <action>
    Create src/components/leaderboard/Leaderboard.tsx:
    - Fetch leaderboard from /api/leaderboard on mount
    - Accept optional userId prop to highlight user's row
    - Period tabs: Weekly, Monthly, All-time
    - Render table with columns: Rank, Trader, Score, Rank Badge
    - Highlight user's row with different background
    - Show empty state if no scores exist
    - Handle loading and error states
    - Display user's rank outside top 100 if applicable
    - Include privacy notice tooltip
  </action>
  <verify>
    <automated>ls src/components/leaderboard/Leaderboard.tsx 2>/dev/null</automated>
  </verify>
  <done>
    Leaderboard displays top 100 traders with period filtering
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Quick Tips Component</name>
  <files>
    - src/components/leaderboard/QuickTips.tsx
  </files>
  <action>
    Create src/components/leaderboard/QuickTips.tsx:
    - Accept scores: EdgeScoreBreakdown prop
    - Import generateQuickTips from @/lib/edge-score
    - Generate tips from scores
    - Render tips in card or panel layout
    - Each tip: icon + text + optional action link
    - Icons by category:
      - discipline: "book-open"
      - risk: "shield-alert"
      - emotional: "brain"
      - consistency: "activity"
      - strategy: "target"
    - Dismiss tips per session (optional)
    - Refresh button to regenerate tips
  </action>
  <verify>
    <automated>ls src/components/leaderboard/QuickTips.tsx 2>/dev/null</automated>
  </verify>
  <done>
    Quick tips component generates and displays improvement tips
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate AI Review Context</name>
  <files>
    - src/app/api/scores/route.ts
  </files>
  <action>
    Update src/app/api/scores/route.ts to integrate AI review (JRNL-06, JRNL-08):
    - When calculating score, also query behavioral analysis from Phase 4
    - Include recurring mistake patterns in quick tips generation
    - Add action plan suggestions based on analysis

    Also create quick tip integration:
    - Import behavioral patterns from trade_analyses if available
    - Inject AI-identified patterns into tips generation
    - Priority: AI patterns > component-based tips
  </action>
  <verify>
    <automated>grep -l "recurring" src/app/api/scores/route.ts 2>/dev/null || echo "needs update"</automated>
  </verify>
  <done>
    Score calculation integrates AI review patterns for contextual tips
  </done>
</task>

</tasks>

<must_haves>
  truths:
    - "Leaderboard API respects user privacy settings (public/anonymous/hidden)"
    - "Leaderboard displays top 100 traders with period filtering"
    - "Quick tips generate from lowest-scoring components"
    - "AI review patterns integrate into quick tips (JRNL-06, JRNL-08)"
    - "User's rank highlights in leaderboard"
  artifacts:
    - path: "src/app/api/leaderboard/route.ts"
      provides: "Privacy-filtered leaderboard data"
      min_lines: 30
    - path: "src/components/leaderboard/Leaderboard.tsx"
      provides: "Leaderboard table UI"
      min_lines: 40
    - path: "src/components/leaderboard/QuickTips.tsx"
      provides: "Improvement tips display"
      min_lines: 30
  key_links:
    - from: "src/app/api/leaderboard/route.ts"
      to: "leaderboard_settings"
      via: "privacy filter join"
      pattern: "leaderboard_settings.*visibility"
    - from: "src/components/leaderboard/QuickTips.tsx"
      to: "src/lib/edge-score"
      via: "generateQuickTips import"
      pattern: "import.*generateQuickTips"
    - from: "src/app/api/scores/route.ts"
      to: "trade_analyses"
      via: "AI pattern integration"
      pattern: "recurring.*mistake"
</must_haves>

<verification>
[Leaderboard filters hidden users]
[Leaderboard maps display names per visibility]
[Quick tips show lowest 2 components]
[Period filtering works: weekly/monthly/all-time]
[User row highlighted in table]
</verification>

<success_criteria>
- [ ] Leaderboard API filters users with visibility='hidden'
- [ ] Leaderboard shows anonymous names for anonymous users
- [ ] Leaderboard period tabs work correctly
- [ ] QuickTips generates tips from scores
- [ ] QuickTips integrates AI patterns from Phase 4
- [ ] User's rank displays highlighted
</success_criteria>

<output>
After completion, create `.planning/phases/06-edge-score/06-04-SUMMARY.md`
</output>