---
phase: "07-education-strategy-lab"
plan: "02"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "supabase/migrations/007_add_courses.sql"
  - "src/app/api/quiz/route.ts"
  - "src/components/education/QuizPlayer.tsx"
  - "src/lib/education/certificates.ts"
autonomous: true
requirements:
  - "EDU-06"
---

<objective>
Quiz System & Certificates. Build the quiz taking system with server-side scoring, immediate feedback, and certificate generation tied to course completion.
</objective>

<context>
@.planning/phases/07-education-strategy-lab/07-CONTEXT.md
@.planning/phases/07-education-strategy-lab/07-RESEARCH.md
@.planning/phases/07-education-strategy-lab/07-01-PLAN.md
</context>

<interfaces>
From src/types/education.ts (per 07-01-PLAN.md):
```typescript
interface Quiz {
  id: string;
  courseId: string;
  questions: QuizQuestion[];
  passingScore: number;
  maxAttempts: number;
}

interface QuizQuestion {
  id: string;
  text: string;
  options: string[];
  correctIndex: number;
  explanation?: string;
}
```

From supabase/migrations (per 07-CONTEXT.md):
```sql
CREATE TABLE quizzes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  course_id UUID REFERENCES courses(id) ON DELETE CASCADE,
  passing_score INT DEFAULT 70,
  max_attempts INT DEFAULT 3
);

CREATE TABLE quiz_questions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  quiz_id UUID REFERENCES quizzes(id) ON DELETE CASCADE,
  question_text TEXT NOT NULL,
  options TEXT[] NOT NULL,
  correct_index INT NOT NULL,
  explanation TEXT,
  order_index INT NOT NULL
);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Quiz database schema and API endpoints</name>
  <files>
    - "supabase/migrations/007_add_courses.sql"
    - "src/app/api/quiz/route.ts"
    - "src/app/api/quiz/submit/route.ts"
  </files>
  <read_first>
    @.planning/phases/03-trade-journal/03-01-PLAN.md 2>/dev/null | head -100
  </read_first>
  <action>
Extend the migration file to add quiz tables (or create 007_add_courses.sql as a companion). Add `quizzes` table and `quiz_questions` table per schema in CONTEXT.md. Add seed data: at least 1 quiz per course track with 5-10 questions each. Use real trading education questions (e.g., "What is proper risk per trade?", "When should you increase position size?").

Create `src/app/api/quiz/route.ts` (GET):
- Accept `quizId` query param
- Fetch quiz with questions from Supabase
- Return quiz data WITHOUT correct answers (correctIndex omitted on client)
- Verify user owns the course (user must have started the course)

Create `src/app/api/quiz/submit/route.ts` (POST):
- Accept JSON body: `{ quizId: string, answers: Record<string, number> }` (questionId → selected option index)
- Fetch quiz with correct answers from Supabase
- Calculate score server-side: count correct / total questions
- Update `course_progress.quiz_score` and increment `quiz_attempts`
- If score >= passingScore AND not already completed: mark `completed_at`, call `issueCertificate()`
- Return: `{ score: number, passed: boolean, passedAnswers: Record<string, { correct: boolean, explanation: string }> }`

CRITICAL: Score calculation MUST happen server-side. Do not trust client-submitted scores.
  </action>
  <verify>
    <automated>ls supabase/migrations/007_add_courses.sql src/app/api/quiz/route.ts src/app/api/quiz/submit/route.ts</automated>
  </verify>
  <done>
Quiz tables exist in Supabase with seed questions, GET /api/quiz returns questions without answers, POST /api/quiz/submit calculates score server-side and awards certificate on pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: QuizPlayer component with immediate feedback</name>
  <files>
    - "src/components/education/QuizPlayer.tsx"
    - "src/components/education/QuizResults.tsx"
    - "src/app/education/quiz/[quizId]/page.tsx"
  </files>
  <read_first>
    @.planning/phases/07-education-strategy-lab/07-RESEARCH.md (Pattern 2: Quiz Form with Validation)
  </read_first>
  <action>
Create `src/components/education/QuizPlayer.tsx`:
- Fetch quiz data from GET /api/quiz?quizId= on mount
- Display quiz title and question count
- Render each question with 4 radio button options (label with hover highlight)
- Use react-hook-form with zod for answer selection validation
- Submit button calls POST /api/quiz/submit with answers object
- On submit success, render QuizResults component
- Track selected answers in form state, show "1 of N answered" counter
- Prevent submit if not all questions answered

Create `src/components/education/QuizResults.tsx`:
- Display score as large percentage number (color: green if passed, red if failed)
- Show pass/fail message with appropriate icon (checkmark or X)
- Display per-question results: question text, selected answer (red), correct answer (green), explanation
- "Retry Quiz" button if failed and attempts < maxAttempts
- "View Certificate" button if passed (links to certificate)
- "Back to Course" button

Create quiz page at `src/app/education/quiz/[quizId]/page.tsx`:
- Middleware: user must be enrolled in course
- Render QuizPlayer component
- Handle loading and error states

Follow Pattern 2 from 07-RESEARCH.md exactly for the form structure.
  </action>
  <verify>
    <automated>ls src/components/education/QuizPlayer.tsx src/components/education/QuizResults.tsx src/app/education/quiz/\[quizId\]/page.tsx && npm test -- --testPathPattern="quiz" --passWithNoTests</automated>
  </verify>
  <done>
Quiz renders with all questions and radio options, submission sends answers to server, server calculates score, results display with per-question feedback showing correct/incorrect with explanations, certificate viewable on pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Certificate display and download page</name>
  <files>
    - "src/app/education/certificates/page.tsx"
    - "src/components/education/CertificateCard.tsx"
    - "src/components/education/CertificatePreview.tsx"
  </files>
  <read_first>
    @.planning/phases/07-education-strategy-lab/07-RESEARCH.md (Pattern 3: PDF Certificate Generation)
  </read_first>
  <action>
Create certificates page at `src/app/education/certificates/page.tsx`:
- Fetch all user's certificates from course_progress where certificate_issued = true
- Display grid of CertificateCard components
- Show empty state if no certificates yet: "Complete a course to earn your first certificate"
- Link to /education/certificates for user dashboard

Create `src/components/education/CertificateCard.tsx`:
- Display certificate thumbnail (preview or placeholder)
- Show course name, completion date
- "Download PDF" button that downloads from certificate_url
- "Share" button (copy link to clipboard)

Create `src/components/education/CertificatePreview.tsx`:
- Canvas preview of the certificate using pdf-lib to render a preview image
- Or: render an HTML/CSS certificate preview that mirrors the PDF design
- Show: "Certificate of Completion", user name, course name, date, certificate ID

Ensure certificate URLs are accessible (Supabase Storage bucket with public read).
  </action>
  <verify>
    <automated>ls src/app/education/certificates/page.tsx src/components/education/CertificateCard.tsx src/components/education/CertificatePreview.tsx</automated>
  </verify>
  <done>
Certificate page shows all earned certificates, each with course name, date, and downloadable PDF. Users can share certificate links.
  </done>
</task>

</tasks>

<must_haves>
truths:
  - "User can take quizzes and receive immediate per-question feedback"
  - "Quiz scores are calculated server-side and cannot be manipulated"
  - "Passing score (70%) awards certificate automatically"
  - "User can download earned certificates as PDF"
artifacts:
  - path: "src/app/api/quiz/submit/route.ts"
    provides: "Server-side quiz scoring and certificate award"
    exports: ["POST handler with score calculation"]
  - path: "src/components/education/QuizPlayer.tsx"
    provides: "Quiz taking interface"
    min_lines: 120
  - path: "src/components/education/QuizResults.tsx"
    provides: "Quiz results with per-question feedback"
    min_lines: 80
  - path: "src/lib/education/certificates.ts"
    provides: "PDF generation (shared from 07-01)"
    exports: ["generateCertificate"]
key_links:
  - from: "src/components/education/QuizPlayer.tsx"
    to: "/api/quiz/submit"
    via: "POST fetch with answers object"
    pattern: "fetch.*api/quiz/submit"
  - from: "/api/quiz/submit"
    to: "course_progress"
    via: "Supabase update + issueCertificate()"
    pattern: "supabase.*from.*course_progress"
  - from: "src/app/education/certificates/page.tsx"
    to: "course_progress.certificate_url"
    via: "fetch certificates with url"
    pattern: "certificate_url.*supabase"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client → API} | Quiz answers submitted for server-side scoring |
| {API → database} | Score validation and certificate issuance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | Quiz score | mitigate | Server-side score calculation in /api/quiz/submit |
| T-07-05 | Tampering | Quiz answers | mitigate | Correct answers never sent to client, only stored in DB |
| T-07-06 | Spoofing | Certificate claim | mitigate | Certificate tied to course_progress record, unique ID |
</threat_model>

<verification>
- Taking a quiz shows questions without correct answers
- Submitting quiz returns server-calculated score
- Passing score triggers certificate generation
- Failed quiz shows per-question feedback with correct answers and explanations
- Certificate PDF downloads with correct user name and course
</verification>

<success_criteria>
- Quiz renders with all questions, client never sees correct answers
- POST /api/quiz/submit calculates score server-side (EDU-06)
- Quiz results show per-question feedback with explanations (EDU-06)
- Certificate generated and downloadable on quiz pass (EDU-06, EDU-05)
- Certificate page displays all user's earned certificates
</success_criteria>

<output>
After completion, create `.planning/phases/07-education-strategy-lab/07-02-SUMMARY.md`
</output>
