---
phase: "07-education-strategy-lab"
plan: "04"
type: "execute"
wave: 3
depends_on:
  - "07-03"
files_modified:
  - "src/lib/strategy-lab/simulation.ts"
  - "src/lib/strategy-lab/metrics.ts"
  - "src/components/strategy-lab/SimulationResults.tsx"
  - "src/app/api/simulation/run/route.ts"
autonomous: true
requirements:
  - "EDU-08"
  - "EDU-09"
  - "EDU-10"
  - "EDU-11"
---

<objective>
Simulation Engine & Testing. Build the strategy simulation engine for session testing, RR optimization, behavioral comparison, and performance metrics display.
</objective>

<context>
@RESEURCES/strategy-lab.md
@.planning/phases/07-education-strategy-lab/07-CONTEXT.md
@.planning/phases/07-education-strategy-lab/07-RESEARCH.md
@.planning/phases/07-education-strategy-lab/07-03-PLAN.md
</context>

<interfaces>
From src/types/strategy-lab.ts (per 07-03-PLAN.md):
```typescript
interface SimulationResult {
  id: string;
  userId: string;
  strategyId: string;
  startDate: Date;
  endDate: Date;
  parameters: SimulationParameters;
  results: SimulationMetrics;
}

interface SimulationParameters {
  pair: string;
  sessionFilter?: string[];
  includeNewsEvents: boolean;
  behaviorRules: BehaviorRule[];
}

interface SimulationMetrics {
  totalTrades: number;
  winRate: number;
  avgRR: number;
  totalPnl: number;
  maxDrawdown: number;
  consistencyScore: number;
  behavioralImpact: BehavioralImpact[];
}

interface BehaviorRule {
  type: 'stop_after_losses' | 'stop_after_wins' | 'cooldown_after_trades' | 'max_daily_trades';
  value: number;
}

interface Candle {
  time: number;
  open: number;
  high: number;
  low: number;
  close: number;
  volume: number;
}

interface Trade {
  entryTime: number;
  entryPrice: number;
  exitTime: number;
  exitPrice: number;
  pnl: number;
  rr: number;
  reason: string;
}
```

From supabase/migrations (per 07-CONTEXT.md):
```sql
CREATE TABLE simulation_results (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  strategy_id UUID REFERENCES strategies(id) ON DELETE CASCADE,
  parameters JSONB NOT NULL,
  results JSONB NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Simulation engine core and session testing</name>
  <files>
    - "supabase/migrations/009_add_simulations.sql"
    - "src/lib/strategy-lab/simulation.ts"
    - "src/lib/strategy-lab/indicators.ts"
    - "src/lib/strategy-lab/session-filters.ts"
  </files>
  <read_first>
    @.planning/phases/07-education-strategy-lab/07-RESEARCH.md (Pattern 4: Simple Simulation Engine)
    @.planning/phases/07-education-strategy-lab/07-RESEARCH.md (Session Filter Logic)
    @.planning/phases/07-education-strategy-lab/07-RESEARCH.md (Behavioral Rule Simulation)
  </read_first>
  <action>
Create migration file `supabase/migrations/009_add_simulations.sql` with simulation_results table per CONTEXT.md schema. Add RLS: users own their simulation results.

Create `src/lib/strategy-lab/indicators.ts`:
- `calculateSMA(candles, period)` - simple moving average
- `calculateEMA(candles, period)` - exponential moving average  
- `calculateRSI(candles, period)` - relative strength index
- `checkMACross(candles, period)` - returns 'bullish' | 'bearish' | null
All per the entry rule conditions from 07-CONTEXT.md (ma_cross, rsi_above, rsi_below).

Create `src/lib/strategy-lab/session-filters.ts`:
- SESSION_HOURS map per RESEARCH.md (sydney, tokyo, london, newyork)
- `filterBySession(candles, sessions)` - returns candles from specified sessions only
- `filterByDateRange(candles, startDate, endDate)` - date range filter
- `getSessionLabel(hour)` - returns session name for given hour

Create `src/lib/strategy-lab/simulation.ts`:
- `runSimulation(candles, entryRules, exitRules, riskRules, initialBalance)` - core backtest loop per Pattern 4 from RESEARCH.md
  - Entry: checks entryRules against candles
  - Position sizing from riskRules (percent_balance, fixed_lot)
  - Exit: checks exitRules (sl, tp, trailing, time)
  - Returns: { trades, balance, metrics }
- `simulateSession(candles, sessionFilter, config)` - wraps runSimulation with session filter
- `optimizeRR(candles, entryRules, riskRules, rrRange)` - tests multiple RR combinations, returns best configuration per EDU-09
- `simulateWithBehavioralRules(candles, rules, simulationFn)` - wraps simulation with behavioral rules per RESEARCH.md, returns originalPnl, withRulesPnl, impact per EDU-10

Include sample data: Generate 1 month of synthetic candle data for EURUSD H1 for demo/testing purposes (use deterministic random walk).

Follow Pattern 4 from 07-RESEARCH.md exactly for the simulation engine.
  </action>
  <verify>
    <automated>ls src/lib/strategy-lab/simulation.ts src/lib/strategy-lab/indicators.ts src/lib/strategy-lab/session-filters.ts && npm test -- --testPathPattern="simulation" --passWithNoTests</automated>
  </verify>
  <done>
Simulation engine runs backtests against candle data, session filtering works, RR optimization returns best configuration, behavioral rules impact calculated and displayed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Simulation API and simulation run page</name>
  <files>
    - "src/app/api/simulation/run/route.ts"
    - "src/app/strategy-lab/simulate/[strategyId]/page.tsx"
    - "src/components/strategy-lab/SimulationControls.tsx"
  </files>
  <read_first>
    @.planning/phases/07-education-strategy-lab/07-RESEARCH.md (Simulation Engine section)
  </read_first>
  <action>
Create `src/app/api/simulation/run/route.ts` (POST):
- Accept: `{ strategyId: string, pair: string, startDate: string, endDate: string, sessionFilter?: string[], behaviorRules?: BehaviorRule[] }`
- Fetch strategy from database
- Load candle data (use bundled sample data for demo, or accept candles in body)
- Run simulation using `simulateSession()` from simulation.ts
- Run RR optimization using `optimizeRR()`
- Run behavioral simulation using `simulateWithBehavioralRules()` if rules provided
- Save results to simulation_results table
- Return: `{ simulationId, metrics, rrOptimization, behavioralComparison }`

Create `src/components/strategy-lab/SimulationControls.tsx`:
- Pair selector (EURUSD, GBPUSD, USDJPY, etc.)
- Date range picker (start/end date)
- Session filter checkboxes (Sydney, Tokyo, London, New York)
- Behavioral rules section (optional): add rule buttons for stop_after_losses, stop_after_wins, cooldown, max_daily_trades
- "Run Simulation" button
- Loading state with progress indicator

Create simulation page at `src/app/strategy-lab/simulate/[strategyId]/page.tsx`:
- Fetch strategy by ID
- Render SimulationControls component
- On simulation complete, render SimulationResults component
- Link from /strategy-lab page "Simulate" button

API endpoint handles timeout (30 seconds max) and returns partial results if incomplete.
  </action>
  <verify>
    <automated>ls src/app/api/simulation/run/route.ts src/app/strategy-lab/simulate/\[strategyId\]/page.tsx src/components/strategy-lab/SimulationControls.tsx</automated>
  </verify>
  <done>
Simulation API runs backtest and returns metrics, simulation page renders with controls, running simulation saves results and displays metrics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Simulation results display with metrics and charts</name>
  <files>
    - "src/components/strategy-lab/SimulationResults.tsx"
    - "src/lib/strategy-lab/metrics.ts"
    - "src/components/strategy-lab/EquityCurveChart.tsx"
    - "src/components/strategy-lab/DrawdownChart.tsx"
    - "src/components/strategy-lab/MetricsSummary.tsx"
    - "src/components/strategy-lab/TradeList.tsx"
  </files>
  <read_first>
    @.planning/phases/07-education-strategy-lab/07-RESEARCH.md (recharts integration)
  </read_first>
  <action>
Create `src/lib/strategy-lab/metrics.ts`:
- `calculateMaxDrawdown(initialBalance, trades)` - returns max drawdown percentage
- `calculateAvgRR(trades)` - average risk reward ratio
- `calculateConsistency(trades)` - returns 0-100 score based on return variance
- `calculateWinRate(trades)` - win rate percentage
- `formatCurrency(pnl)` - format PnL with currency symbol
- `calculateBehavioralImpact(original, withRules)` - calculate impact delta

Create `src/components/strategy-lab/MetricsSummary.tsx`:
- Grid of metric cards: Total Trades, Win Rate, Avg RR, Total PnL, Max Drawdown, Consistency Score
- Color coding: green for positive metrics, red for negative
- Compact number formatting (e.g., "1,234" not "1234.00")

Create `src/components/strategy-lab/EquityCurveChart.tsx`:
- Line chart using recharts
- X-axis: trade number or date
- Y-axis: balance
- Tooltip with trade details on hover
- Area fill under the line

Create `src/components/strategy-lab/DrawdownChart.tsx`:
- Area chart showing drawdown over time
- Red fill for drawdown periods
- Max drawdown annotation

Create `src/components/strategy-lab/TradeList.tsx`:
- Table showing individual trades from simulation
- Columns: #, Entry Time, Exit Time, PnL, RR, Exit Reason
- Color-coded PnL (green/red)
- Sortable by PnL column

Create `src/components/strategy-lab/SimulationResults.tsx`:
- Tabbed interface: Overview, Equity Curve, Drawdown, Trades
- Overview tab: MetricsSummary + RR optimization results + behavioral comparison
- Equity Curve tab: EquityCurveChart
- Drawdown tab: DrawdownChart  
- Trades tab: TradeList
- "Save Results" button, "Run New Simulation" link back to controls
- Behavioral comparison section showing: original PnL, with rules PnL, impact (+/-%)
  </action>
  <verify>
    <automated>ls src/components/strategy-lab/SimulationResults.tsx src/lib/strategy-lab/metrics.ts src/components/strategy-lab/EquityCurveChart.tsx && npm test -- --testPathPattern="metrics" --passWithNoTests</automated>
  </verify>
  <done>
Simulation results render with all 4 tabs (Overview, Equity Curve, Drawdown, Trades), metrics display correctly, equity curve and drawdown charts render with recharts, behavioral comparison shows impact delta.
  </done>
</task>

</tasks>

<must_haves>
truths:
  - "User can run strategy simulation across different session windows"
  - "User can test multiple RR combinations and see which performs best"
  - "User can compare strategy performance with and without behavioral rules"
  - "User can view simulated PnL, max drawdown, and consistency metrics"
artifacts:
  - path: "src/lib/strategy-lab/simulation.ts"
    provides: "Core simulation engine and backtest logic"
    exports: ["runSimulation", "simulateSession", "optimizeRR", "simulateWithBehavioralRules"]
    min_lines: 200
  - path: "src/lib/strategy-lab/metrics.ts"
    provides: "Performance metric calculations"
    exports: ["calculateMaxDrawdown", "calculateAvgRR", "calculateConsistency"]
  - path: "src/components/strategy-lab/SimulationResults.tsx"
    provides: "Simulation results display with tabs"
    min_lines: 150
  - path: "src/app/api/simulation/run/route.ts"
    provides: "Simulation execution API"
    exports: ["POST handler"]
key_links:
  - from: "src/components/strategy-lab/SimulationControls.tsx"
    to: "/api/simulation/run"
    via: "POST fetch with parameters"
    pattern: "fetch.*api/simulation/run"
  - from: "/api/simulation/run"
    to: "src/lib/strategy-lab/simulation.ts"
    via: "simulateSession() call"
    pattern: "simulateSession.*await"
  - from: "src/components/strategy-lab/SimulationResults.tsx"
    to: "src/lib/strategy-lab/metrics.ts"
    via: "metric calculation functions"
    pattern: "calculateMaxDrawdown"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client → API} | Simulation parameters and strategy data cross here |
| {API → computation} | Simulation runs server-side or Edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Denial of Service | Simulation | mitigate | 30-second timeout, max 10000 candles per run |
| T-07-10 | Information Disclosure | Simulation results | mitigate | RLS on simulation_results: user_id = auth.uid() |
</threat_model>

<verification>
- Running simulation with sample data produces results
- RR optimization returns multiple configurations with metrics
- Behavioral comparison shows original vs. with-rules PnL difference
- All charts render correctly with recharts
- Simulation results saved to database and retrievable
</verification>

<success_criteria>
- Session testing runs strategy across market conditions (EDU-08)
- RR optimization tests multiple combinations (EDU-09)
- Behavioral comparison shows impact of rules (EDU-10)
- Strategy performance metrics display (simulated PnL, max drawdown, consistency) (EDU-11)
</success_criteria>

<output>
After completion, create `.planning/phases/07-education-strategy-lab/07-04-SUMMARY.md`
</output>
