---
phase: 02-trader-dna
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - DNA-01
  - DNA-02
  - DNA-03
  - DNA-04
  - DNA-05
  - DNA-06

must_haves:
  truths:
    - "Assessment questions exist in code with 5 sections"
    - "Question data structure supports 4 question types"
    - "Scoring algorithm calculates 5 section scores"
    - "Profile type is determinable from section scores"
    - "Learning path derives from learning style score"
  artifacts:
    - path: "src/types/trader-dna.ts"
      provides: "TypeScript interfaces for TraderProfile, Question, AnswerMap"
      exports: ["TraderProfile", "ProfileType", "AssessmentQuestion", "SectionKey"]
    - path: "src/data/trader-dna/questions.ts"
      provides: "All 40 assessment questions across 5 sections"
      contains: "questions array with 8 questions per section"
    - path: "src/data/trader-dna/scoring.ts"
      provides: "Profile calculation logic"
      exports: ["calculateProfile", "calculateSectionScores", "determineProfileType"]
  key_links:
    - from: "src/data/trader-dna/questions.ts"
      to: "src/types/trader-dna.ts"
      via: "import AssessmentQuestion type"
      pattern: "export.*AssessmentQuestion"
    - from: "src/data/trader-dna/scoring.ts"
      to: "src/data/trader-dna/questions.ts"
      via: "import questions constant"
      pattern: "import.*questions.*from"
---

<objective>
Create TypeScript types, question data, and scoring algorithm for the Trader DNA assessment system.

Purpose: Foundation layer for assessment — defines the data model and calculation logic that UI components will consume.
</objective>

<context>
@.planning/phases/02-trader-dna/02-CONTEXT.md
@.planning/phases/02-trader-dna/02-RESEARCH.md

# Key interfaces to create (from CONTEXT.md):

TraderProfile interface:
```typescript
interface TraderProfile {
  type: 'sniper' | 'analyst' | 'warrior' | 'disciplinarian' | 'opportunist';
  scores: {
    riskPersonality: number;
    emotionalStability: number;
    decisionMaking: number;
    tradingBehavior: number;
    learningStyle: number;
  };
  recommendations: string[];
  dashboardLayout: string;
  alertThresholds: AlertThresholds;
}
```

AssessmentQuestion interface:
```typescript
interface AssessmentQuestion {
  id: string;
  section: 'risk_personality' | 'emotional_stability' | 'decision_making' | 'trading_behavior' | 'learning_style';
  type: 'multiple_choice' | 'rating' | 'scenario' | 'frequency';
  question: string;
  options: QuestionOption[];
  weight: number;
}
```

Profile types (from CONTEXT.md):
- The Sniper: Patient, high conviction, detailed analysis emphasis
- The Analyst: Data-driven, systematic, charts/metrics emphasis
- The Warrior: Aggressive, confident, quick actions emphasis
- The Disciplinarian: Strict rules, consistent, checklists emphasis
- The Opportunist: Adaptive, flexible, multiple tools emphasis
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript interfaces for Trader DNA</name>
  <files>src/types/trader-dna.ts</files>
  <read_first></read_first>
  <action>
Create src/types/trader-dna.ts with the following exports:

1. SectionKey type: union type of 'riskPersonality' | 'emotionalStability' | 'decisionMaking' | 'tradingBehavior' | 'learningStyle'

2. ProfileType type: union type of 'sniper' | 'analyst' | 'warrior' | 'disciplinarian' | 'opportunist'

3. QuestionType type: 'multiple_choice' | 'rating' | 'scenario' | 'frequency'

4. QuestionOption interface: { value: number | string; label: string; description?: string }

5. AssessmentQuestion interface: { id: string; section: SectionKey; type: QuestionType; question: string; options: QuestionOption[]; weight: number }

6. AnswerMap type: Record<string, number | string>

7. Section interface: { title: string; description: string; questionCount: number }

8. AlertThresholds interface: { riskSensitivity: 'low' | 'medium' | 'high'; alertFrequency: 'minimal' | 'normal' | 'detailed' }

9. DashboardLayout interface: { primaryWidget: string; widgetOrder: string[] }

10. TraderProfile interface with: type, scores (5 section scores 0-100), learningPath, dashboardLayout, alertThresholds, recommendations

11. sections constant: Record<SectionKey, Section> with 5 sections, 8 questions each, titles from CONTEXT.md
  </action>
  <verify>
    <automated>grep -c "^export" src/types/trader-dna.ts</automated>
  </verify>
  <done>
    - All 11 exports present in src/types/trader-dna.ts
    - TraderProfile interface matches CONTEXT.md specification
    - sections constant has 5 entries with questionCount: 8 each
  </done>
</task>

<task type="auto">
  <name>Task 2: Create assessment question data with 40 questions</name>
  <files>src/data/trader-dna/questions.ts</files>
  <read_first>src/types/trader-dna.ts</read_first>
  <action>
Create src/data/trader-dna/questions.ts that exports:

1. Import AssessmentQuestion and SectionKey from types

2. Create questions array with exactly 40 AssessmentQuestion objects (8 per section):
   - Section 'riskPersonality' (8 questions): scenario and rating types about aggression, risk appetite, loss tolerance, recovery
   - Section 'emotionalStability' (8 questions): scenario types about impulsiveness, patience, frustration, revenge tendencies
   - Section 'decisionMaking' (8 questions): scenario types about analytical, emotional, reactive, structured decision styles
   - Section 'tradingBehavior' (8 questions): frequency types about overtrading, FOMO, discipline, consistency
   - Section 'learningStyle' (8 questions): multiple_choice types about visual, structured, practical preferences

3. Each question MUST have: id (format: section-abbrev-number, e.g., 'risk-1', 'emot-1', 'dec-1', 'beh-1', 'learn-1'), section, type, question text, options array with 3-5 options, weight between 1.0 and 2.0

4. Export sections constant matching the types file
  </action>
  <verify>
    <automated>grep -c "section: 'riskPersonality'" src/data/trader-dna/questions.ts</automated>
  </verify>
  <done>
    - 40 questions total in questions array
    - 8 questions per section (verify with grep count per section)
    - Question types: multiple_choice, rating, scenario, frequency all present
    - All question IDs unique
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement scoring algorithm for profile calculation</name>
  <files>src/data/trader-dna/scoring.ts</files>
  <read_first>src/types/trader-dna.ts, src/data/trader-dna/questions.ts</read_first>
  <action>
Create src/data/trader-dna/scoring.ts with the following functions:

1. Import TraderProfile, ProfileType, AnswerMap, SectionKey from types

2. calculateSectionScores(answers: AnswerMap): Record<SectionKey, number> - Calculate weighted sum per section, normalize to 0-100 scale

3. determineProfileType(scores: Record<SectionKey, number>): ProfileType - Map dominant section to profile type:
   - If riskPersonality > 70: 'warrior'
   - If riskPersonality <= 70 and high: 'sniper'
   - If decisionMaking > 60: 'analyst'
   - If tradingBehavior > 70: 'disciplinarian'
   - If emotionalStability < 50: 'opportunist'
   - Default fallback: 'analyst'

4. determineLearningPath(learningStyleScore: number): 'visual' | 'structured' | 'practical' - Based on score thresholds

5. getDashboardLayout(profileType: ProfileType): DashboardLayout - Return layout config per CONTEXT.md

6. getAlertThresholds(profileType: ProfileType): AlertThresholds - Return thresholds per CONTEXT.md

7. generateRecommendations(scores: Record<SectionKey, number>, profileType: ProfileType): string[] - Generate 3-5 recommendations

8. calculateProfile(answers: AnswerMap): TraderProfile - Main function that orchestrates all calculations and returns complete profile

9. Export all functions and add getTraderProfile type for fetching from DB
  </action>
  <verify>
    <automated>grep -c "^export" src/data/trader-dna/scoring.ts</automated>
  </verify>
  <done>
    - All 8 functions exported
    - calculateProfile returns TraderProfile with all required fields
    - Profile types correctly mapped from dominant section
    - Dashboard layout and alert thresholds match CONTEXT.md specifications
  </done>
</task>

</tasks>

<verification>
- TypeScript compilation succeeds: `npm run build` (will be tested in later phase)
- All types imported correctly in questions.ts and scoring.ts
- Questions array has exactly 40 items (8 per section)
- Scoring functions handle edge cases (empty answers, mid-range scores)
</verification>

<success_criteria>
- DNA-01: Question data structure exists with 5 sections
- DNA-02 through DNA-06: Each section has 8 relevant questions
- DNA-07: Profile calculation logic determines type from scores
- DNA-09: Learning path derivation function exists
</success_criteria>

<output>
After completion, create `.planning/phases/02-trader-dna/02-01-SUMMARY.md`
</output>