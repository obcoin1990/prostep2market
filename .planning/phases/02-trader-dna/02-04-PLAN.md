---
phase: 02-trader-dna
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified: []
autonomous: true
requirements:
  - DNA-08
  - DNA-09
  - DNA-10

must_haves:
  truths:
    - "Profile badge displays on dashboard header/sidebar"
    - "Dashboard widget layout varies by profile type"
    - "Learning path recommendation shows on dashboard"
    - "User can view full profile from dashboard"
  artifacts:
    - path: "src/components/trader-dna/profile-badge.tsx"
      provides: "Small profile indicator for dashboard"
      exports: ["ProfileBadge"]
    - path: "src/components/trader-dna/learning-path-display.tsx"
      provides: "Learning path recommendation component"
      exports: ["LearningPathDisplay"]
    - path: "src/app/(dashboard)/profile/page.tsx"
      provides: "Full profile page (DNA-10)"
    - path: "src/components/dashboard/personalized-layout.tsx"
      provides: "Dashboard layout engine based on profile"
  key_links:
    - from: "src/components/trader-dna/profile-badge.tsx"
      to: "src/lib/trader-profile.ts"
      via: "import getTraderProfile"
      pattern: "import.*getTraderProfile.*from"
    - from: "src/components/dashboard/personalized-layout.tsx"
      to: "src/types/trader-dna.ts"
      via: "import DashboardLayout"
      pattern: "import.*DashboardLayout.*from"
---

<objective>
Implement dashboard personalization based on Trader DNA profile - profile badge, layout variations, learning path display.

Purpose: Connect profile data to dashboard UI for DNA-08, DNA-09, DNA-10 requirements.
</objective>

<context>
@.planning/phases/02-trader-dna/02-CONTEXT.md
@.planning/phases/02-trader-dna/02-RESEARCH.md
@.planning/phases/02-trader-dna/02-03-PLAN.md

# Dashboard layouts per profile type (CONTEXT.md):

- The Sniper: Detailed analysis widgets emphasis
- The Analyst: Charts, metrics, reports emphasis
- The Warrior: Quick actions, shortcuts emphasis
- The Disciplinarian: Checklists, adherence metrics emphasis
- The Opportunist: Multiple tools, quick switching emphasis

# Alert thresholds per profile (CONTEXT.md):

- Sniper: Medium sensitivity, normal frequency
- Analyst: High sensitivity, detailed frequency
- Warrior: Low sensitivity, minimal frequency
- Disciplinarian: High sensitivity, normal frequency
- Opportunist: Medium sensitivity, normal frequency

# Profile page should show:
- Profile type and description
- All 5 section scores with visual bars
- Learning path recommendation
- Retake assessment option
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProfileBadge component for dashboard</name>
  <files>src/components/trader-dna/profile-badge.tsx</files>
 <read_first>src/types/trader-dna.ts</read_first>
 <action>
Create src/components/trader-dna/profile-badge.tsx:

1. Accepts: profile: TraderProfile prop (or null if no profile yet)

2. Renders:
   - If no profile: show "Complete Assessment" prompt with CTA button
   - If profile exists: show badge with:
     - Profile type icon/emoji (sniper: target, analyst: chart, warrior: shield, disciplinarian: checklist, opportunist: tools)
     - Profile name (e.g., "The Sniper")
     - Subtle styling with brand colors

3. Profile type icons:
   - sniper: "🎯"
   - analyst: "📊"
   - warrior: "⚔️"
   - disciplinarian: "📋"
   - opportunist: "🔄"

4. Hover state shows tooltip with brief description
5. Click navigates to /profile page (full profile view)
  </action>
 <verify>
<automated>grep -c "export" src/components/trader-dna/profile-badge.tsx</automated>
 </verify>
 <done>
   - Badge shows for existing profiles
   - Prompt shows for missing profiles
   - Click navigates to profile page
 </done>
</task>

<task type="auto">
  <name>Task 2: Create LearningPathDisplay component</name>
 <files>src/components/trader-dna/learning-path-display.tsx</files>
 <read_first>src/types/trader-dna.ts</read_first>
 <action>
Create src/components/trader-dna/learning-path-display.tsx:

1. Accepts: learningPath: 'visual' | 'structured' | 'practical' prop

2. Renders recommendation based on learning path:
   - visual: "Your learning path: Visual Learning - Watch video lessons, study charts, observe traders"
   - structured: "Your learning path: Structured Learning - Read documentation, follow courses in order, complete quizzes"
   - practical: "Your learning path: Practical Learning - Practice with simulations, test strategies, learn by doing"

3. Display format:
   - Card with icon, title, description
   - "Recommended for your profile" label
   - Link to Phase 7 education (future) - for now just display text

4. Use brand styling (border-left accent, appropriate spacing)
  </action>
 <verify>
<automated>grep -c "export" src/components/trader-dna/learning-path-display.tsx</automated>
 </verify>
 <done>
   - Shows appropriate learning path based on score
   - Card format with icon and description
 </done>
</task>

<task type="auto">
  <name>Task 3: Create full profile page</name>
 <files>src/app/(dashboard)/profile/page.tsx, src/app/(dashboard)/profile/page.client.tsx</files>
 <read_first>src/components/trader-dna/profile-summary.tsx, src/lib/trader-profile.ts</read_first>
 <action>
Create profile page:

1. src/app/(dashboard)/profile/page.tsx:
   - Server component
   - Check authentication (redirect if not logged in)
   - Call getTraderProfile() server action
   - Pass profile to client component

2. src/app/(dashboard)/profile/page.client.tsx:
   - 'use client'
   - If no profile: show "You haven't completed the Trader DNA assessment yet" + CTA to /trader-dna
   - If profile: reuse ProfileSummary component with full profile data

3. Page title: "My Trader Profile"
4. Add breadcrumbs or back navigation to dashboard
  </action>
 <verify>
<automated>ls -la src/app/\\(dashboard\\)/profile/</automated>
 </verify>
 <done>
   - Profile page accessible at /dashboard/profile
   - Shows summary if profile exists
   - Prompts to complete if no profile
 </done>
</task>

<task type="auto">
<name>Task 4: Create personalized dashboard layout component</name>
 <files>src/components/dashboard/personalized-layout.tsx</files>
 <read_first>src/types/trader-dna.ts</read_first>
 <action>
Create src/components/dashboard/personalized-layout.tsx:

1. Accepts: profile: TraderProfile | null prop

2. Define widget order per profile type (from CONTEXT.md):
   ```typescript
   const LAYOUTS: Record<ProfileType, string[]> = {
     sniper: ['analysis', 'performance', 'alerts', 'risk'],
     analyst: ['metrics', 'charts', 'performance', 'alerts'],
     warrior: ['quickActions', 'alerts', 'journal', 'performance'],
     disciplinarian: ['checklists', 'adherence', 'performance', 'metrics'],
     opportunist: ['tools', 'alerts', 'journal', 'performance']
   }
   ```

3. If profile exists:
   - Return widget order from LAYOUTS[profile.type]
   - Return primaryWidget from profile.dashboardLayout.primaryWidget
   - Return alertThresholds from profile.alertThresholds

4. If no profile:
   - Return default layout (suitable for new users)
   - Return default alert thresholds

5. Export functions:
   - getWidgetOrder(profile): string[]
   - getPrimaryWidget(profile): string
   - getAlertThresholds(profile): AlertThresholds
  </action>
 <verify>
<automated>grep -c "^export" src/components/dashboard/personalized-layout.tsx</automated>
 </verify>
 <done>
   - Returns correct widget order per profile type
   - Handles null profile gracefully with defaults
 </done>
</task>

<task type="auto">
<name>Task 5: Integrate profile badge into dashboard header</name>
 <files>src/app/(dashboard)/layout.tsx</files>
 <read_first>src/app/(dashboard)/layout.tsx (if exists from Phase 1)</read_first>
 <action>
Add ProfileBadge to dashboard layout:

1. If layout.tsx exists from Phase 1:
   - Import ProfileBadge component
   - Add to header area (near user avatar)
   - Pass profile data (fetch via getTraderProfile)

2. If layout.tsx doesn't exist:
   - Create basic layout with sidebar/header structure
   - Include ProfileBadge in header
   - Include basic navigation

3. Also integrate LearningPathDisplay into dashboard:
   - Show on dashboard home if profile exists
   - Place in a widget area or below main widgets

Note: This is a placeholder integration - actual widget arrangement will be implemented in Phase 3-6 as widgets are built.
  </action>
 <verify>
<parameter name="automated">grep -c "ProfileBadge\|LearningPathDisplay" src/app/\(dashboard\)/layout.tsx 2>/dev/null || echo "layout may not exist yet"</automated>
 </verify>
 <done>
   - Profile badge appears on dashboard
   - Learning path recommendation shows
   - Null profile handled gracefully
 </done>
</task>

</tasks>

<verification>
- Profile badge displays in dashboard header
- Learning path shows on dashboard
- Profile page accessible at /profile
- Dashboard layout uses profile data (widget order)
- Alert thresholds configured per profile type
</verification>

<success_criteria>
- DNA-08: Dashboard layout varies by profile type
- DNA-09: Learning path recommendation displays on dashboard
- DNA-10: Profile summary accessible from profile page
- DNA-08: Alert thresholds configured based on profile
</success_criteria>

<output>
After completion, create `.planning/phases/02-trader-dna/02-04-SUMMARY.md`
</output>