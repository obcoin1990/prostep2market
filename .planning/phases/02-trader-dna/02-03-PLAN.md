---
phase: 02-trader-dna
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified: []
autonomous: true
requirements:
  - DNA-07
  - DNA-10

must_haves:
  truths:
    - "Profile saves to Supabase after assessment completion"
    - "Profile can be retrieved by authenticated user"
    - "Users can only access their own profile (RLS)"
    - "Profile can be updated if user re-takes assessment"
  artifacts:
    - path: "supabase/migrations/YYYYMMDD_create_trader_profiles.sql"
      provides: "Database schema for trader_profiles table"
      creates: "table with profile_type, 5 scores, learning_path, dashboard_layout, alert_thresholds"
    - path: "src/lib/trader-profile.ts"
      provides: "Server actions for profile CRUD"
      exports: ["saveTraderProfile", "getTraderProfile"]
    - path: "src/components/trader-dna/profile-summary.tsx"
      provides: "DNA-10: Profile display component"
      exports: ["ProfileSummary"]
  key_links:
    - from: "src/lib/trader-profile.ts"
      to: "src/components/trader-dna/assessment-wizard.tsx"
      via: "import saveTraderProfile"
      pattern: "import.*saveTraderProfile.*from"
    - from: "src/components/trader-dna/profile-summary.tsx"
      to: "src/lib/trader-profile.ts"
      via: "import getTraderProfile"
      pattern: "import.*getTraderProfile.*from"
---

<objective>
Implement database schema, server actions, and profile summary display for Trader DNA.

Purpose: Persist profile to Supabase and display it back to the user.
</objective>

<context>
@.planning/phases/02-trader-dna/02-CONTEXT.md
@.planning/phases/02-trader-dna/02-RESEARCH.md
@.planning/phases/02-trader-dna/02-02-PLAN.md

# Database schema from CONTEXT.md:

CREATE TABLE trader_profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id),
  profile_type TEXT NOT NULL CHECK (profile_type IN ('sniper', 'analyst', 'warrior', 'disciplinarian', 'opportunist')),
  risk_personality_score INT CHECK (risk_personality_score BETWEEN 0 AND 100),
  emotional_stability_score INT CHECK (emotional_stability_score BETWEEN 0 AND 100),
  decision_making_score INT CHECK (decision_making_score BETWEEN 0 AND 100),
  trading_behavior_score INT CHECK (trading_behavior_score BETWEEN 0 AND 100),
  learning_style_score INT CHECK (learning_style_score BETWEEN 0 AND 100),
  learning_path TEXT CHECK (learning_path IN ('visual', 'structured', 'practical')),
  dashboard_layout JSONB DEFAULT '{"widgetOrder": ["performance", "alerts", "metrics"]}',
  alert_thresholds JSONB DEFAULT '{"riskSensitivity": "medium", "alertFrequency": "normal"}',
  completed_at TIMESTAMPTZ DEFAULT NOW(),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

# RLS policies:
- SELECT: auth.uid() = id
- INSERT: auth.uid() = id
- UPDATE: auth.uid() = id

# Server action pattern from RESEARCH.md:

export async function saveTraderProfile(profile: TraderProfile) {
  // upsert with onConflict: 'id'
}

export async function getTraderProfile() {
  // select where id = user.id
}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database migration for trader_profiles table</name>
  <files>supabase/migrations/20260508_create_trader_profiles.sql</files>
  <read_first></read_first>
  <action>
Create supabase/migrations/20260508_create_trader_profiles.sql:

1. Create table trader_profiles:
   - id: UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE
   - profile_type: TEXT NOT NULL CHECK (profile_type IN ('sniper', 'analyst', 'warrior', 'disciplinarian', 'opportunist'))
   - risk_personality_score: INT CHECK (BETWEEN 0 AND 100)
   - emotional_stability_score: INT CHECK (BETWEEN 0 AND 100)
   - decision_making_score: INT CHECK (BETWEEN 0 AND 100)
   - trading_behavior_score: INT CHECK (BETWEEN 0 AND 100)
   - learning_style_score: INT CHECK (BETWEEN 0 AND 100)
   - learning_path: TEXT CHECK (IN ('visual', 'structured', 'practical'))
   - dashboard_layout: JSONB DEFAULT with default widget order
   - alert_thresholds: JSONB DEFAULT with default thresholds
   - completed_at: TIMESTAMPTZ DEFAULT NOW()
   - created_at: TIMESTAMPTZ DEFAULT NOW()

2. Enable RLS on table

3. Create RLS policies:
   - "Users can read own profile": SELECT to authenticated USING (auth.uid() = id)
   - "Users can create own profile": INSERT to authenticated WITH CHECK (auth.uid() = id)
   - "Users can update own profile": UPDATE to authenticated USING (auth.uid() = id)

4. Add comment explaining table purpose
  </action>
  <verify>
<parameter name="automated">grep -c "CREATE TABLE\|CREATE POLICY" supabase/migrations/20260508_create_trader_profiles.sql</automated>
  </verify>
  <done>
    - trader_profiles table created with all columns
    - RLS enabled with 3 policies
    - CHECK constraints on profile_type and scores
  </done>
</task>

<task type="auto">
  <name>Task 2: Create server actions for profile CRUD</name>
  <files>src/lib/trader-profile.ts</files>
  <read_first>src/types/trader-dna.ts</read_first>
  <action>
Create src/lib/trader-profile.ts:

1. 'use server' directive

2. Import: createClient from @/lib/supabase/server, TraderProfile from @/types/trader-dna

3. saveTraderProfile(profile: TraderProfile):
   - Get authenticated user via supabase.auth.getUser()
   - If no user, throw Error('Not authenticated')
   - Upsert to trader_profiles table with:
     - id: user.id
     - profile_type: profile.type
     - risk_personality_score: profile.scores.riskPersonality
     - emotional_stability_score: profile.scores.emotionalStability
     - decision_making_score: profile.scores.decisionMaking
     - trading_behavior_score: profile.scores.tradingBehavior
     - learning_style_score: profile.scores.learningStyle
     - learning_path: profile.learningPath
     - dashboard_layout: JSON.stringify(profile.dashboardLayout)
     - alert_thresholds: JSON.stringify(profile.alertThresholds)
     - completed_at: new Date().toISOString()
   - Use onConflict: 'id' for upsert behavior
   - Handle errors and throw if failed

4. getTraderProfile():
   - Get authenticated user
   - If no user, return null
   - Select * from trader_profiles where id = user.id
   - Return data or null if not found

5. getProfileWithDefaults():
   - Calls getTraderProfile()
   - If profile exists, maps DB columns to TraderProfile interface
   - If no profile, returns default profile with 'analyst' type and default scores
  </action>
  <verify>
<parameter name="automated">grep -c "^export" src/lib/trader-profile.ts</automated>
  </verify>
  <done>
    - saveTraderProfile performs upsert (create or update)
    - getTraderProfile returns profile or null
    - Both handle auth properly
  </done>
</task>

<task type="auto">
  <name>Task 3: Connect wizard to save profile on completion</name>
  <files>src/components/trader-dna/assessment-wizard.tsx</files>
  <read_first>src/components/trader-dna/assessment-wizard.tsx, src/lib/trader-profile.ts</read_first>
  <action>
Modify assessment-wizard.tsx:

1. Import saveTraderProfile from @/lib/trader-profile

2. Update handleNext function:
   - After calculating profile, call await saveTraderProfile(calculatedProfile)
   - Wrap in try/catch to handle save errors
   - If save fails, still show profile but show warning message

3. Add loading state during save:
   - Add saving state (boolean)
   - Show "Saving..." text on button during save
   - Disable buttons during save

4. Handle save error gracefully:
   - Log error to console
   - Allow user to proceed to results despite save failure
   - Show "Profile saved" or "Profile generated" status
  </action>
  <verify>
<automated>grep -c "saveTraderProfile" src/components/trader-dna/assessment-wizard.tsx</automated>
  </verify>
  <done>
    - Profile saves to Supabase after calculation
    - User sees save status
    - Error handling prevents blocking on save failure
  </done>
</task>

<task type="auto">
  <name>Task 4: Create ProfileSummary component</name>
  <files>src/components/trader-dna/profile-summary.tsx</files>
  <read_first>src/types/trader-dna.ts</read_first>
  <action>
Create src/components/trader-dna/profile-summary.tsx:

1. Accepts: profile: TraderProfile prop

2. Renders:
   - Profile type badge (e.g., "The Sniper" with icon/color)
   - Profile description based on type (2-3 sentences from CONTEXT.md)
   - Scores display (5 section scores as progress bars 0-100)
   - Learning path recommendation
   - "Retake Assessment" button (optional, links back to /trader-dna)
   - "Go to Dashboard" button

3. Profile type display names:
   - sniper: "The Sniper"
   - analyst: "The Analyst"
   - warrior: "The Warrior"
   - disciplinarian: "The Disciplinarian"
   - opportunist: "The Opportunist"

4. Use brand colors: brand-red for primary accent, progress bars show score percentage

5. Make it responsive (mobile-friendly)
  </action>
  <verify>
<parameter name="automated">grep -c "export" src/components/trader-dna/profile-summary.tsx</automated>
  </verify>
  <done>
    - Shows profile type prominently with badge
    - Displays all 5 section scores
    - Shows learning path recommendation
    - Navigation buttons work
  </done>
</task>

<task type="auto">
  <name>Task 5: Update wizard to render ProfileSummary on completion</name>
  <files>src/components/trader-dna/assessment-wizard.tsx</files>
  <read_first>src/components/trader-dna/assessment-wizard.tsx, src/components/trader-dna/profile-summary.tsx</read_first>
  <action>
Modify assessment-wizard.tsx:

1. Import ProfileSummary from ./profile-summary

2. Update render logic:
   - When isComplete && profile, render <ProfileSummary profile={profile} /> instead of placeholder
   - This completes the flow: assessment → calculation → save → display
  </action>
  <verify>
<parameter name="automated">grep -c "ProfileSummary" src/components/trader-dna/assessment-wizard.tsx</automated>
  </verify>
  <done>
    - Assessment completes and shows profile summary
    - Full flow works: questions → calculate → save → display
  </done>
</task>

</tasks>

<verification>
- Database migration creates table with proper schema
- RLS policies prevent cross-user access
- Server actions save and retrieve correctly
- ProfileSummary renders complete profile data
- Full end-to-end flow works (wizard → save → display)
</verification>

<success_criteria>
- DNA-07: Profile persists to Supabase after assessment
- DNA-07: Profile can be retrieved and displayed
- DNA-10: Profile summary shows type, scores, recommendations
</success_criteria>

<output>
After completion, create `.planning/phases/02-trader-dna/02-03-SUMMARY.md`
</output>