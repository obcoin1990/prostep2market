// PDF generation is handled server-side via pdf-lib in:
// src/app/api/certificates/[id]/pdf/route.ts
//
// This file is intentionally empty — the CertificatePreview component
// (src/components/certificate/CertificatePreview.tsx) handles the
// on-screen HTML preview and download trigger.
export {}
